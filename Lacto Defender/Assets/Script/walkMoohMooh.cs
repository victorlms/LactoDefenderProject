using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class walkMoohMooh : MonoBehaviour {

	/*
	Vector2 _mousePosition;
	bool isEmpty;
	public bool prepara = false;
	public bool prepara2 = false;
	Vector4 backColor;



	Card card;

	void Start () {
		card = gameObject.transform.GetComponent<Card> ();
	}

	void Update () {
		
		_mousePosition = Input.mousePosition;
		Ray ray = Camera.main.ScreenPointToRay (Input.mousePosition);
		RaycastHit hit;
		/*
		if (Physics.Raycast (ray, out hit)) {
			if (hit.collider.gameObject == gameObject) {
				if (Input.GetMouseButtonDown (0) && (transform.GetComponent<spawnPlayer> ().posiciona == false)) {
					prepara = true;
				}
			}
		}

		if (prepara) {

			foreach (GameObject campo in card.walk) {
				if (Physics.Raycast (ray, out hit)) {
					if (hit.collider.gameObject == campo.gameObject) {
						if (Input.GetMouseButtonDown (0)) {
							prepara2 = true;
							if (campo.transform.GetComponent<ScriptField> ().freeFloor == true) {
								gameObject.transform.position = Vector2.Lerp (transform.position, campo.transform.position, Time.deltaTime);
							}
						}
					}
				}
			}
			



		}
	}

	void OnMouseDown(){

		if (transform.GetComponent<spawnPlayer> ().posiciona == false) {
			prepara = true;
		}

	}*/
}
